# byte-model can be shared or split
# symbol-bits can be 4 or 8
# max-total defines the total frequency limit (default: 16384)
# max-total can be specified globally or once per phase
# alpha defines the pseudocount as n/d (e.g., 1/2)
# alpha can be specified globally or once per phase
# any number of (x,y) contexts can be defined
# a single context line applies globally
# four context lines apply per phase
# phase 0 corresponds to 1 0 0 0, the first symbol emitted from the pixel
byte-model=shared
symbol-bits=4
max-total=8191
max-total=8191
max-total=2047
max-total=511
alpha=1/20
alpha=1/2
alpha=1/2
alpha=1/1
context=(-4,0),(8,-1),(0,-1),(0,-2),(0,-4)
context=(-1,0),(-4,0),(-5,0),(0,-1),(-1,-1)
context=(-1,0),(-4,0),(-5,0),(-9,0),(11,-1),(-5,-1)
context=(-1,0),(-20,0)
